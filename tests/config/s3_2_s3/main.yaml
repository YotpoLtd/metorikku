# The MQL file path
metrics:
  - ./main_metric.yaml

inputs:
  persons_s3:
    file:
      path: s3://test-bucket/persons.json

output:
  file:
    dir: s3://test-bucket/output

explain: true

logLevel: WARN

appName: TestApp

showPreviewLines: 10
