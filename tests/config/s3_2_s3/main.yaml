# The MQL file path
metrics:
  - tests/config/s3_2_s3/main_metric.yaml

inputs:
  persons_s3:
    file:
      path: s3://test-bucket/persons.json

output:
  file:
    dir: s3://test-bucket/output

explain: true

logLevel: WARN

appName: TestApp

showPreviewLines: 10
