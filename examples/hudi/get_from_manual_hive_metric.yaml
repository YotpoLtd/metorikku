steps:
- dataFrameName: manual_hive_sync_test
  sql:
    SELECT movieId,
           rating
    FROM hive_sync_test
    WHERE userId=1
- dataFrameName: manual_hive_sync_test_rt
  sql:
    SELECT movieId,
           rating
    FROM hive_sync_test_rt
    WHERE userId=1