steps:
- dataFrameName: manual_hive_sync_test
  sql:
    SELECT movieId,
           rating
    FROM manual_hive_sync
    WHERE userId=1
- dataFrameName: manual_hive_sync_test_rt
  sql:
    SELECT movieId,
           rating
    FROM manual_hive_sync_rt
    WHERE userId=1