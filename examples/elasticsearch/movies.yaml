metrics:
  - examples/elasticsearch/movies_metric.yaml

inputs:
  movies:
    file:
      path: examples/mocks/movies.jsonl
  ratings:
    file:
      path: examples/mocks/ratings.jsonl
output:
  elasticsearch:
    nodes: elasticsearch:9200
